
  # Figma Prototype for socialpAInt (Copy)

  This is a code bundle for Figma Prototype for socialpAInt (Copy). The original project is available at https://www.figma.com/design/6VXeOYaf4wDTM7yYlK4gK6/Figma-Prototype-for-socialpAInt--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  